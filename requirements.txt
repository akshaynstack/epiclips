# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
pydantic-settings==2.1.0

# AWS
boto3==1.34.0

# Video/Image Processing
opencv-python-headless==4.9.0.80
numpy>=1.26.3,<2.0          # Pin to 1.x for compatibility with opencv/mediapipe
Pillow==10.2.0
ffmpeg-python==0.2.0        # FFmpeg bindings for rendering
scipy>=1.12.0,<1.14         # Pin to version compatible with numpy 1.x

# ML Models
# NOTE: torch/torchvision are installed FIRST in Dockerfile with CPU-only index
# (--index-url https://download.pytorch.org/whl/cpu) to avoid 4GB of CUDA deps.
# Version pins here ensure pip doesn't try to upgrade when resolving ultralytics deps.
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
ultralytics==8.1.0          # YOLOv8 (uses torch installed above)
mediapipe==0.10.9           # Pose estimation
deep-sort-realtime==1.3.2   # Object tracking

# Transcription (Groq Whisper)
groq>=0.4.2                 # Groq API (fast Whisper transcription)

# yt-dlp for video download
yt-dlp>=2024.1.0
yt-dlp-ejs
rnet                        # Browser impersonation for YouTube anti-bot bypass

# Utilities
httpx==0.26.0               # Async HTTP client
python-json-logger==2.0.7
aiofiles>=23.0.0            # Async file operations

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0



