# =============================================================================
# Epiclips - Production Requirements (Optimized)
# =============================================================================
# Removed heavy unused dependencies for ~2x faster builds:
# - google-generativeai: NOT USED (Gemini accessed via OpenRouter API with httpx)
# - scipy: NOT USED directly in app code  
# - pytest: Moved to requirements-dev.txt
# =============================================================================

# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
pydantic-settings==2.1.0

# AWS
boto3==1.34.0

# Video/Image Processing
opencv-python-headless==4.9.0.80
numpy>=1.26.3,<2.0          # Pin to 1.x for compatibility with opencv/mediapipe
Pillow==10.2.0
ffmpeg-python==0.2.0        # FFmpeg bindings for rendering

# ML Models
mediapipe==0.10.9           # Pose estimation & face detection
deep-sort-realtime==1.3.2   # Object tracking

# Speaker Diarization (CPU-only)
scikit-learn>=1.3.0         # Agglomerative clustering for speaker identification

# Transcription (Groq Whisper + Local Whisper)
groq>=0.4.2                 # Groq API (fast Whisper transcription)
faster-whisper              # Local Whisper engine (CPU/GPU)

# yt-dlp for video download
yt-dlp>=2024.1.0

# OPTIONAL: Browser impersonation for YouTube anti-bot bypass
# These are optional - yt-dlp will work without them but may hit rate limits
# rnet                      # Browser impersonation for YouTube anti-bot bypass
# PySocks                   # SOCKS proxy support for yt-dlp

# Utilities
httpx==0.26.0               # Async HTTP client
python-json-logger==2.0.7
python-dotenv               # Load environment variables from .env
aiofiles>=23.0.0            # Async file operations
